apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: minio-tenant
  namespace: gitops
spec:
  project: default
  source:
    repoURL: 'https://github.com/Lorran/ws-stack-dados-k8s.git'
    path: infra/src/helm-charts/minio-tenant
    targetRevision: Dev-Digital-Ocean
    helm:
      valueFiles:
        - values.yaml
  destination:
      name: 'do-nyc1-pool-k8s'
      namespace: deepstorage
      server: ''
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
